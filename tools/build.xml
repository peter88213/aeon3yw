<?xml version="1.0" encoding="UTF-8"?>
<project name="aeon3yw" basedir=".">
	<property name="release" value="0.1.6" />
	<property name="src" location="../src" />
	<property name="test" location="../test" />
	<property name="build" location="../build" />
	<property name="dist" location="../dist" />
	<property name="docs" location="../docs" />
	<property name="project-name" value="${ant.project.name}" />
	<property name="release-name" value="${project-name}_v${release}" />

	<target name="build" description="inline all library modules">
		<delete file="${test}/aeon3yw.pyw" />
		<exec executable="python" failonerror="true">
		    <arg value="${src}/build_aeon3yw.py"/>
		</exec>
		<fixcrlf encoding="utf-8" eol="lf" srcdir="${test}" includes="**/*.pyw" />
	</target>

	<target name="test" description="inline all library modules">
		<exec executable="python" failonerror="true">
		    <arg value="${test}/test_aeon3yw.py"/>
		</exec>
	</target>

	<target name="dist" description="generate the distribution">
		
		<delete dir="${build}" />
		<mkdir dir="${build}" />
		<delete dir="${dist}" />
		<mkdir dir="${dist}" />
		<mkdir dir="${build}/${release-name}" />

		<copy todir="${build}/${release-name}/sample"> 
			<fileset dir="../sample" />
		</copy>
		
		<copy file="./install.bat" todir="${build}/${release-name}" />
		
		<copy file="${test}/aeon3yw.pyw" tofile="${build}/${release-name}/aeon3yw.pyw" />
		<replace encoding="utf-8" file="${build}/${release-name}/aeon3yw.pyw" token="@release" value="${release}" />

		<copy file="../LICENSE" todir="${build}/${release-name}" />
		<copy file="${docs}/usage.md" tofile="${build}/${release-name}/README.md" />

		<zip destfile="${dist}/${release-name}.zip" basedir="${build}/${release-name}" />

		<copy file="${docs}/template/index.md" overwrite="True" todir="${docs}" />
		<replace encoding="utf-8" file="${docs}/index.md" token="0.99.0" value="${release}" />

	</target>

	<target name="clean" description="clean up">
		
		<delete file="${test}/aeon3yw.pyw" />
		
	</target>

</project>